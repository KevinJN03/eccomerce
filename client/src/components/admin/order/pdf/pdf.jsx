import PackingSlip from './packingSlip.';

function Pdf({}) {
    return (
        <section className="flex items-center justify-center">
            <PackingSlip />
        </section>
    );
}

export default Pdf;
