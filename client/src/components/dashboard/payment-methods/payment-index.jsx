import { Outlet } from 'react-router-dom';

function Payment_Method_Index({}) {
    return (
        <section className="payment-method">
            <Outlet />
        </section>
    );
}

export default Payment_Method_Index;
